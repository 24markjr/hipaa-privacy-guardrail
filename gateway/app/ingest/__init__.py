"""Document ingestion — extract plain text from uploaded files.

Extracted text flows through the exact same compliance pipeline as a pasted
note, so PHI protection is identical. Image/scanned-doc OCR is intentionally
out of scope (heavier + accuracy/privacy risks).
"""
